{
  "name": "blackjack-lambda",
  "version": "1.0.0",
  "description": "Serverless blackjack game Lambda function",
  "main": "blackjack.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "blackjack",
    "game"
  ],
  "author": "Joe Leto",
  "license": "MIT"
}
